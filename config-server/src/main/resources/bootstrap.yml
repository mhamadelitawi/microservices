spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/mhamadelitawi/IdeaProjects/microservices/config-server-repository
          clone-on-start: true
    fail-fast: true